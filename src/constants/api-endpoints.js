const topics = 'topics'
const indexPage = 'index_page'
const indexPageCategories = 'index_page_categories'
const posts = 'posts'

export default {
  topics,
  indexPage,
  indexPageCategories,
  posts,
}
